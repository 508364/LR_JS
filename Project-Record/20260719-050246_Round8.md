# LR_JS 继续修复优化 — 去 V8/QuickJS 化 + 重构 — Round 8

**时间**: 2026-07-19 05:02  
**任务**: 移除构建文档/源码中的 V8/QuickJS 引用，重命名 V8 性能优化符号，增强项目独立性

---

## 一、文档和构建系统去 V8/QuickJS 化

### 1.1 docs/API.md 修改
- 概述行移除"基于 QuickJS 引擎" → "内建高性能 JS 引擎"
- MSVC 构建命令中 `quickjs` 路径 → `engine`
- 参考链接移除 QuickJS 和 V8 链接

### 1.2 Makefile 重构
全部 QuickJS 变量名改为通用引擎名：

| 原变量名 | 新变量名 |
|---------|---------|
| `QJS_DIR` | `ENGINE_DIR` |
| `QJS_SRCS` | `ENGINE_SRCS` |
| `QJS_OBJS` | `ENGINE_OBJS` |
| `QJS_CFLAGS` | `ENGINE_CFLAGS` |
| 注释 "Compile QuickJS" | "Compile engine" |
| 注释 "QuickJS source" | "Engine source" |
| 注释 "built on QuickJS" | 移除 |

## 二、V8 性能优化模块重命名

### 2.1 文件重命名
- `src/lr_v8_opt.h` → `src/lr_perf_opt.h`（新建）
- `src/lr_v8_opt.c` → `src/lr_perf_opt.c`（新建）
- 删除旧 `src/lr_v8_opt.h`、`src/lr_v8_opt.c`

### 2.2 符号重命名

| 原符号 | 新符号 |
|--------|--------|
| `LR_V8OptCtx` | `LR_PerfOptCtx` |
| `LR_V8BCEntry` | `LR_PerfBCEntry` |
| `lr_v8opt_init()` | `lr_perfopt_init()` |
| `lr_v8opt_destroy()` | `lr_perfopt_destroy()` |
| `lr_v8opt_attach()` | `lr_perfopt_attach()` |
| `lr_v8opt_detach()` | `lr_perfopt_detach()` |
| `lr_v8opt_print_stats()` | `lr_perfopt_print_stats()` |
| `enable_v8_optimizations` | `enable_perf_optimizations` |

### 2.3 引用更新
- `src/lr_runtime.h` — `#include "lr_v8_opt.h"` → `#include "lr_perf_opt.h"`，字段/函数声明更新
- `include/lr_js.h` — `enable_v8_optimizations` → `enable_perf_optimizations`，注释更新
- `Makefile` — SOURCES 列表中 `lr_v8_opt.c` → `lr_perf_opt.c`

## 三、源码注释去 QuickJS/V8 化

| 文件 | 修改内容 |
|------|---------|
| `src/lr_runtime.c` | `based on QuickJS engine` → `high-performance JS runtime` |
| `src/lr_runtime.c` | `QuickJS engine` → `lightweight JS engine` |
| `src/lr_runtime.c` | `Create QuickJS runtime` → `Create JS runtime` |
| `src/lr_runtime.c` | `Free QuickJS` → `Free JS runtime` |
| `src/lr_gc.c` | `QuickJS` → `JS engine`（7处注释） |
| `src/lr_thread_pool.c` | `QuickJS runtime` → `JS runtime` |
| `src/lr_thread_pool.h` | `QuickJS` → `JS`（2处） |
| `src/lr_sandbox.c` | `QuickJS runtime` → `JS runtime` |
| `src/lr_timers.c` | `QuickJS` → `engine` |
| `src/lr_runtime.h` | `V8-inspired optimizations` → `Performance optimizations` |
| `src/lr_runtime.h` | `V8 optimization helpers` → `Performance optimization helpers` |
| `include/lr_js.h` | `V8-inspired optimizations` → `Performance optimizations` |

**注意**: `#include "quickjs.h"` 和 `qjs_*` 字段名保留不变 — 这些是实际引擎 API 引用，非品牌名称。

## 四、额外修复

- `src/lr_gc.c` 注释中 `*/` 提前闭合导致编译错误 → 修复注释格式

## 五、验证结果

- 编译通过（无错误）
- 全部功能测试通过（URL、Event、removeEventListener、console.group、TextEncoder、btoa/atob、localStorage、Promise、crypto、performance、ES2022 features）
- 内存压力测试通过（10000 对象分配/释放）
- 原有测试套件全部通过
- 退出码 0

## 涉及文件

- `src/lr_perf_opt.h` — 新建（替代 lr_v8_opt.h）
- `src/lr_perf_opt.c` — 新建（替代 lr_v8_opt.c）
- `src/lr_v8_opt.h` — 删除
- `src/lr_v8_opt.c` — 删除
- `src/lr_runtime.h` — 符号引用更新 + 注释更新
- `src/lr_runtime.c` — 注释更新
- `include/lr_js.h` — 符号 + 注释更新
- `Makefile` — 变量重命名 + 源文件列表更新
- `docs/API.md` — 移除 QuickJS/V8 引用
- `src/lr_gc.c` — 注释修复 + 注释更新
- `src/lr_thread_pool.c` — 注释更新
- `src/lr_thread_pool.h` — 注释更新
- `src/lr_sandbox.c` — 注释更新
- `src/lr_timers.c` — 注释更新

## Git 黑名单
`.gitignore` 包含 `quickjs/` 和 `v8/`，Project-Record 文件夹无需上传